#端口
server:
  port: 1111
# 别名
spring:
  application:
    name: eureka-server-01

# eureka config
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:2222/eureka/,http://localhost:3333/eureka/

